{foreach from=$News item=New}				
<article> 
 <header>
  <h1>{$New.titre}</h1>
  <p class="amap-info">Message publié le : <span>{$New.date|date_format:"%d/%m/%Y"}</span> à <span>{$New.date|date_format:"%H:%M"}</span></p>
 </header>
	<p>{$New.contenu}</p>
</article>
{/foreach}

