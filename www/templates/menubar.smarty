	<div class="row">
		<div class="col-md-12">
			<ul class="nav">
{foreach from=$Menu item=line}
  {if $line.type eq 'up'}
				<li class="nav-item {if $smarty.foreach.line.first}active{/if}">
					<a class="nav-link head-menu" href="#" onclick="loadContent('{$line.link}');return false;">{$line.label}</a>
				</li>
  {/if}
{/foreach}
			</ul>			
		</div>
	</div>
